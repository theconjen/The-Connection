/**
 * CREATE TAB - The Connection Mobile App
 * ----------------------------------------
 * This is a placeholder tab that gets hidden from the tab bar.
 * The actual create functionality is handled by the center button
 * in the tab bar which opens the fan menu.
 */

import { View } from 'react-native';

export default function CreateTab() {
  // This screen should never actually be shown since href: null
  return <View />;
}
