// Canonical domain types shared by web and mobile
// Type-only re-exports from shared/schema ensure zero runtime impact in web bundles
export type { User, Post, Community, Event, EventRsvp } from '../schema';
