/// <reference types="vitest" />

// This file exists to expose Vitest types to the repo-wide TypeScript build.
// It is intentionally minimal; adding it avoids tsc errors when consuming Vitest globals.
