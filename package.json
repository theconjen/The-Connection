{
  "name": "esbuild-test",
  "scripts": {
    "build": "esbuild server/index.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist-server/index.js --external:*",
    "watch": "esbuild src/app.ts --bundle --sourcemap --outdir=www --servedir=www --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  },
  "engines": {
    "node": "22.x"
  },
  "packageManager": "pnpm@10.19.0"
}
