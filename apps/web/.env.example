# Web App Environment Variables (Example)
# Copy this file to .env for local overrides or set in Vercel dashboard for deployments

# API Base URL
# - For Vercel deployments: Use /api (enables proxy via vercel.json rewrites)
# - For local development: See .env.development for dev API URL
# - For native apps: The code automatically uses https://api.theconnection.app
VITE_API_BASE=/api
